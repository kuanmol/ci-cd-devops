kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
    extraPortMappings:
      - containerPort: 30080   # NodePort from your Service
        hostPort: 30080        # Expose it on localhost
        protocol: TCP
  - role: worker
  - role: worker
